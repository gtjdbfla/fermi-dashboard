[server]
headless = true
# 컨테이너 밖에서 접속하려면 루프백이 아니라 모든 인터페이스에 바인딩해야 한다.
address = "0.0.0.0"
port = 8502

[browser]
gatherUsageStats = false

# [theme] 섹션을 두지 않는다. 하나라도 지정하면 Streamlit이 커스텀 테마로 보고
# 우측 상단 메뉴의 라이트/다크/시스템 선택이 제한된다.
# 차트 색은 theme.palette()가 st.context.theme을 읽어 따라간다.
